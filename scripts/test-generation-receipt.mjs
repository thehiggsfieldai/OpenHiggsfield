import assert from 'node:assert/strict';import {generationReceipt,receiptMetadata} from '../deploy/beta/generation-receipt.mjs';const id='11111111-1111-1111-1111-111111111111';for(const status_url of ['https://platform.higgsfield.ai/requests/'+id+'/status','https://api.higgsfield.ai/requests/'+id+'/status?opaque=secret','https://untrusted.invalid/callback','not-a-url']){const receipt=generationReceipt({request_id:id,status_url,status:'queued'},{id:'local',model:'seedance-2.5'});assert.equal(receipt.status_url,'https://api.higgsfield.ai/requests/'+id+'/status');}assert.equal(generationReceipt({status_url:'https://platform.higgsfield.ai/requests/'+id+'/status'},{id:'local',model:'seedance-2.5'}).request_id,id);assert.throws(()=>generationReceipt({status_url:'https://evil.invalid/requests/'+id+'/status'},{id:'local',model:'seedance-2.5'}));assert.throws(()=>generationReceipt({request_id:'../../private'},{id:'local',model:'seedance-2.5'}));assert.ok(!JSON.stringify(receiptMetadata({request_id:id,status_url:'https://api.higgsfield.ai/path?secret=token'})).includes('token'));console.log('PASS: provider job IDs normalize to the fixed status endpoint, URL variants do not lose valid IDs, unsafe IDs fail and receipt metadata omits query secrets.');
